---
- name: "install google-chrome"
  aur:
    use: trizen
    name: google-chrome
  become: yes
  become_user: vagrant
- name: "install chrome remote desktop"
  aur:
    use: trizen
    name: chrome-remote-desktop
  become: yes
  become_user: vagrant
